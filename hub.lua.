--[[
    ZL PVP V0.1
    Asegúrate de copiar TODO este código y pegarlo en tu archivo de GitHub.
]]

local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/78n/Kavo-UI-Library/main/source.lua"))()

local Window = Library:CreateWindow({
    Title = "ZL PVP V0.1",
    Theme = {
        MainColor = Color3.fromRGB(20, 20, 20),
        AccentColor = Color3.fromRGB(212, 175, 55)
    },
    Size = Vector2.new(220, 220)
})

-- Pestañas
local Tab1 = Window:CreateTab("Com")
local Tab2 = Window:CreateTab("Mov")
local Tab3 = Window:CreateTab("Uti")
local Tab4 = Window:CreateTab("Key")

-- Movimiento
Tab2:CreateSlider("Velocidad", 16, 250, 16, function(v)
    game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = v
end)

Tab2:CreateToggle("Salto Infinito", function(state)
    _G.InfJump = state
    game:GetService("UserInputService").JumpRequest:Connect(function()
        if _G.InfJump then
            game.Players.LocalPlayer.Character:FindFirstChildOfClass('Humanoid'):ChangeState("Jumping")
        end
    end)
end)

-- Utilidades
Tab3:CreateButton("Activar ESP", function()
    print("ESP Activado")
end)

-- Créditos
Tab4:CreateLabel("Owner: ZL")
Tab4:CreateLabel("Versión: 0.1")
